# Maintainer: Franklin <jahdy1o3@gmail.com>
pkgname=modforge-cli
pkgver=0.2.7
pkgrel=3
pkgdesc="ModForge-CLI — a Modrinth-based Minecraft modpack builder"
arch=('any')
url="https://github.com/Frank1o3/ModForge-CLI"
license=('MIT')
depends=(
    'python'
    'python-requests'
    'python-jsonschema'
    'python-aiofiles'
    'python-pydantic'
    'python-rich'
    'python-aiohttp'
    'python-pyfiglet'
    'python-typer'
)
# pyzipper not in official repos — bundled via pip
optdepends=()
conflicts=()
provides=('modforge-cli')
source=("modforge_cli-${pkgver}-py3-none-any.whl::https://github.com/Frank1o3/ModForge-CLI/releases/download/v${pkgver}/modforge_cli-${pkgver}-py3-none-any.whl")
sha256sums=('SKIP')

package() {
    # Install pyzipper separately since it's not in official repos
    pip install \
        --isolated \
        --root="${pkgdir}" \
        --ignore-installed \
        --prefix=/usr \
        --no-deps \
        pyzipper

    # Install modforge-cli
    pip install \
        --isolated \
        --root="${pkgdir}" \
        --ignore-installed \
        --prefix=/usr \
        --no-deps \
        "${srcdir}/modforge_cli-${pkgver}-py3-none-any.whl"
}
